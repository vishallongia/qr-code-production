<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Not Found | Dashboard</title>
    <link rel="icon" href="/app-icon-192.png" type="image/png" />
    <link rel="stylesheet" href="/css/add-quiz-question.css" />
    <style>
        :root {
            --blue: #18024b;
            --greygrey-6: rgba(30, 29, 33, 1);
            --block-size: 40px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #f4f2f7 0%, #ffffff 100%);
            overflow: hidden;
            position: relative;
        }

        .not-found-container {
            text-align: center;
            z-index: 2;
            position: relative;
            padding: 20px;
        }

        .not-found-title {
            font-size: 100px;
            font-weight: 900;
            color: var(--blue);
            margin-bottom: 10px;
            position: relative;
            animation: bounce 1.5s ease infinite;
        }

        .not-found-subtitle {
            font-size: 22px;
            font-weight: 500;
            color: var(--greygrey-6);
            margin-bottom: 10px;
        }

        .not-found-text {
            font-size: 16px;
            color: rgba(51, 51, 51, 0.7);
            max-width: 400px;
            margin: 0 auto 24px;
            line-height: 1.6;
        }

        .btn-home {
            background-color: var(--blue);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 10px 24px;
            font-size: 16px;
            cursor: pointer;
            text-decoration: none;
            transition: 0.3s all;
            display: inline-block;
        }

        .btn-home:hover {
            background-color: rgba(26, 1, 97, 0.9);
        }

        /* Falling blocks */
        .falling-block {
            width: var(--block-size);
            height: var(--block-size);
            background-color: var(--blue);
            position: absolute;
            top: -50px;
            left: 0;
            opacity: 0.8;
            border-radius: 4px;
            animation: fall linear infinite;
        }

        @keyframes fall {
            0% {
                transform: translateY(0) rotate(0deg);
                opacity: 0.8;
            }

            50% {
                transform: translateY(50vh) rotate(180deg);
                opacity: 0.6;
            }

            100% {
                transform: translateY(100vh) rotate(360deg);
                opacity: 0;
            }
        }

        @keyframes bounce {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-15px);
            }
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .not-found-title {
                font-size: 80px;
            }

            .not-found-subtitle {
                font-size: 20px;
            }
        }

        @media (max-width: 768px) {
            .not-found-title {
                font-size: 60px;
            }

            .not-found-subtitle {
                font-size: 18px;
            }

            .not-found-text {
                font-size: 14px;
            }
        }

        @media (max-width: 480px) {
            .not-found-title {
                font-size: 40px;
            }

            .not-found-subtitle {
                font-size: 16px;
            }

            .not-found-text {
                font-size: 13px;
                max-width: 90%;
            }
        }
    </style>
</head>

<body>
    <div class="not-found-container">
        <h1 class="not-found-title">404</h1>
        <p class="not-found-subtitle">Page Not Found</p>
        <p class="not-found-text">
            The page you are trying to access doesn’t exist or may have been moved.
        </p>
        <a href="/" class="btn-home">← Back to Dashboard</a>
    </div>

    <script>
        const blockCount = 20; // Number of blocks
        for (let i = 0; i < blockCount; i++) {
            const block = document.createElement('div');
            block.classList.add('falling-block');
            const size = window.innerWidth < 480 ? 15 + Math.random() * 15 : 20 + Math.random() * 40;
            block.style.width = block.style.height = size + 'px';
            block.style.left = Math.random() * window.innerWidth + 'px';
            block.style.backgroundColor = `hsl(${Math.random() * 360}, 80%, 50%)`; // colorful blocks
            block.style.animationDuration = 3 + Math.random() * 3 + 's';
            block.style.animationDelay = Math.random() * 5 + 's';
            document.body.appendChild(block);
        }
    </script>
</body>

</html>