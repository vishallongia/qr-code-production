<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Safe ID Variant Session</title>
    <link rel="stylesheet" href="/css/add-quiz-question.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet" />
    <link rel="icon" type="image/png" sizes="192x192" href="/app-icon-192.png">

    <style>
        .preview-img {

    background: #18024b;
}
    </style>
</head>

<body>

    <div class="loader-overlay" id="loader">
        <div class="spinner"></div>
    </div>
    <% if (error) { %>
        <div class="quiz-error-message">
            <%= error %>
        </div>
        <% } else { %>
            <div class="quiz-container">
                <div style="text-align: center;">
                    <img class="event-icon" src="<%= safeVariant.questionImage %>" alt="">
                </div>

                <h2>Safe ID Creator</h2>
                <form id="edit-question-form" enctype="multipart/form-data">
                    <input type="hidden" name="safeVariantId" value="<%= safeVariant._id %>" />
                    <input type="hidden" name="safeId" value="<%= safeId %>" />
                    <input type="hidden" id="clearedImagesInput" name="clearedImages" />
                    <input type="hidden" id="clearedOptions" name="clearedOptions" />


                    <div class="quiz-question-actions">
                        <button type="button" class="quiz-btn-collapse"
                            onclick="window.open('/safe-id/safe/variant/<%= safeVariant._id %>/play?lang=en', '_blank')">
                            Show Me the Frontend
                        </button>
                        <button type="button" class="quiz-btn-collapse"
                            onclick="window.open('/authenticateqr?type=safe-id-variant&linked=true&variantId=<%= safeVariant._id %>&sessionId=true', '_blank')">
                            Send to a Magic Code
                        </button>
                        <button type="button" class="quiz-btn-collapse"
                            onclick="window.open('/dashboard?type=safe-id-variant&linked=true&variantId=<%= safeVariant._id %>&sessionId=true', '_blank')">
                            Send to New Magic Code
                        </button>
                        <% if (safeVariant.linkedQRCode) { %>
                            <button type="button" class="quiz-btn-collapse" data-question-id="<%= safeVariant._id %>"
                                id="unlinkBtn">
                                Unsend
                            </button>
                            <% } %>
                                <button type="button" class="quiz-btn-collapse"
                                    onclick="window.open('/safe-id/variant/<%= safeVariant._id %>', '_blank')">
                                    Show the Magic Code of this Event
                                </button>

                    </div>

                    <div class="quiz-form-group">
                        <div style="display:none; justify-content:space-between; align-items:center;">
                            <label>Toggle What to Show on Front End Header</label>
                            <button type="submit" class="quiz-btn-collapse">
                                Save Front End Header
                            </button>
                        </div>
                        <div class="quiz-media-profile-toggles" style="display: none;">
                            <div class="quiz-toggle-wrapper">
                                <label class="quiz-toggle-label">Safe ID Profile</label>
                                <label class="quiz-toggle-switch">
                                    <input type="checkbox" id="safeIdToggle" name="showSafeIdProfile"
                                        <%=safeVariant.showSafeIdProfile ? 'checked' : '' %> />
                                    <span class="quiz-toggle-slider"></span>
                                </label>
                                <button type="button" class="quiz-btn-submit" style="margin:2px 0px; padding: 4px 8px;"
                                    id="edit-broadcaster"
                                    onclick="window.open('/safe-id/safeids-list?id=<%= safeId %>', '_blank')">Edit</button>
                            </div>
                        </div>

                    </div>
                    <div id="logoParentContainer" style="<%= Array.isArray(safeVariant.logoMediaProfile) && safeVariant.logoMediaProfile.includes('custom') 
       ? '' 
       : 'display: none;' %>">
                        <div class="quiz-form-group">
                            <label>Name of Your Channel </label>
                            <input type="text" name="logoTitle" value="<%= safeVariant.logoTitle %>"
                                placeholder="Enter logo title" />
                        </div>

                        <div class="quiz-form-group">
                            <label>Description of Your Channel Logo </label>
                            <input type="text" name="logoDescription" value="<%= safeVariant.logoDescription %>"
                                placeholder="Enter logo description" />
                        </div>

                        <div class="quiz-form-group">
                            <label>Logo Link</label>
                            <input type="url" name="logoLink" placeholder="https://example.com"
                                value="<%= safeVariant.logoLink %>" />
                        </div>
                        <div class="quiz-form-group">
                            <label>Upload Your Channel Logo </label>
                            <label class="quiz-file-label">
                                <span class="quiz-file-label-text">
                                    <%= safeVariant.logo ? safeVariant.logo.split('/').pop() : 'Choose File' %>
                                </span>
                                <input type="file" name="logo" class="quiz-logoImage quiz-hidden-file"
                                    accept="image/*" />
                            </label>
                            <% if (safeVariant.logo) { %>
                                <img class="preview-img quiz-logoImagePreview" src="<%= safeVariant.logo %>"
                                    style="display: block;" />
                                <% } else { %>
                                    <img class="preview-img quiz-logoImagePreview" style="display: none;" />
                                    <% } %>
                        </div>
                    </div>

                    <div>
                        <div class="quiz-form-group">
                            <label>Item Name (Where Your Safe ID is Placed)</label>
                            <input type="text" name="question" placeholder="Item Name (Where Your Safe ID is Placed)"
                                value="<%= safeVariant.question || '' %>" />
                        </div>
                        <div class="quiz-form-group">
                            <label>Item Image</label>
                            <label class="quiz-file-label">
                                <span class="quiz-file-label-text">
                                    <%= safeVariant.questionImage ? safeVariant.questionImage.split('/').pop()
                                        : 'Choose File' %>
                                </span>
                                <input type="file" name="questionImage" class="quiz-hidden-file" accept="image/*" />
                            </label>
                            <% if (safeVariant.questionImage) { %>
                                <img class="preview-img" src="<%= safeVariant.questionImage %>"
                                    style="display: block;" />
                                <% } else { %>
                                    <img class="preview-img" style="display: none;" />
                                    <% } %>
                        </div>
                        <div class="quiz-form-group">
                            <label>Description of Your Item </label>
                            <input type="text" name="questionDescription"
                                value="<%= safeVariant.questionDescription || '' %>" placeholder="Description of Your Item " />
                        </div>



                        <div class="quiz-form-group">
                            <label  style="font-weight: bold;">Add a Message to Your Safe ID</label>
                            <input type="text" name="questionMessage" value="<%= safeVariant.questionMessage || '' %>"
                                placeholder="Add a Message to Your Safe ID" />
                        </div>
                    </div>

                    <!-- ⭐ NEW CONTACT DETAILS SECTION ⭐ -->
                    <!-- ⭐ NEW CONTACT DETAILS SECTION ⭐ -->
                    <div class="quiz-form-group">
                        <label>1. General Phone Number</label>
                        <input type="tel" name="generalPhoneNumber" placeholder="+"
                            value="<%= safeVariant.generalPhoneNumber || '' %>" />
                    </div>

                    <div class="quiz-form-group">
                        <label>2. Emergency Phone Number</label>
                        <input type="tel" name="emergencyPhoneNumber" placeholder="+"
                            value="<%= safeVariant.emergencyPhoneNumber || '' %>" />
                    </div>

                    <div class="quiz-form-group">
                        <label>3. Other Phone Number</label>
                        <input type="tel" name="otherPhoneNumber" placeholder="+"
                            value="<%= safeVariant.otherPhoneNumber || '' %>" />
                    </div>
                    <!-- ⭐ END CONTACT DETAILS ⭐ -->

                    <!-- ⭐ END CONTACT DETAILS ⭐ -->
                    <div class="quiz-option-wrapper">



                        <div id="option-blocks" class="quiz-option-blocks quiz-options-grid">
                            <% safeVariant.options.forEach((opt, index)=> { %>
                                <div class="quiz-option-block" data-option-id="<%= opt._id %>">
                                    <label>Add Messenger App </label>



                                    <!-- Option text fields -->
                                    <input type="text" name="optionTexts[]" value="<%= opt.text %>"
                                        placeholder="Name of the App" />
                                    <input type="text" name="optionDescriptions[]" value="<%= opt.description || '' %>"
                                        placeholder="Description" />
                                    <input type="url" name="optionLinks[]" value="<%= opt.link || '' %>"
                                        placeholder="Link" />

                                    <!-- ✅ Selected logo/image preview -->
                                    <% if (opt.selectedLogoSrc) { %>
                                        <img class="quiz-selected-logo-preview" src="<%= opt.selectedLogoSrc %>"
                                            style="display:block; max-width:120px; margin-top:10px;" />
                                        <% } else if (opt.image) { %>
                                            <img class="quiz-selected-logo-preview" src="<%= opt.image %>"
                                                style="display:block; max-width:120px; margin-top:10px;" />
                                            <% } else { %>
                                                <img class="quiz-selected-logo-preview"
                                                    style="display:none; max-width:120px; margin-top:10px;" />
                                                <% } %>

                                                    <!-- ✅ Logo container (populated onload by JS, but space reserved here) -->
                                                    <div class="quiz-option-logos"
                                                        data-selected-logo="<%= opt.selectedLogoSrc || '' %>"></div>



                                                    <!-- File upload -->
                                                    <label class="quiz-file-label">
                                                        <span class="quiz-file-label-text">
                                                            <%= opt.image ? opt.image.split('/').pop() : 'Choose Image'
                                                                %>
                                                        </span>
                                                        <input type="file" class="quiz-hidden-file option-image-input"
                                                            name="optionImages" accept="image/*" />
                                                    </label>

                                                    <!-- ✕ delete -->
                                                    <button type="button" class="quiz-option-delete-btn">✕</button>
                                </div>


                                <% }); %>
                        </div>
                        <div class="quiz-option-controls">
                            <button type="button" id="btn-add-option" class="quiz-btn-add-option">+ Add
                                Chat</button>
                        </div>
                        <div class="quiz-form-group">
                            <label>Email</label>
                            <input type="email" name="email" placeholder="yourname@example.com"
                                value="<%= safeVariant.email || '' %>" />
                        </div>
                    </div>
                    <button type="submit" class="quiz-btn-submit">Save</button>
                </form>
            </div>
            <% } %>

                <script src="https://cdnjs.cloudflare.com/ajax/libs/toastify-js/1.12.0/toastify.min.js"></script>
                <script src="/js/toastify-setup.js"></script>
                <script src="/js/edit-safe-variant.js"></script>
</body>

</html>