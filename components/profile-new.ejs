<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>My Profile Page</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="/css/profile-new.css" rel="stylesheet">
</head>

<body>
    <div class="container-custom-profile">
        <h1 data-translate="my_profile">My Profile</h1>


        <form class="form-container" id="profileForm" method="POST" onsubmit="return handleUpdateDetailsProfile(event)">
            <div class="form-fields">
                <div class="form-group">
                    <label for="fullName" data-translate="full_name">Full Name</label>
                    <input type="text" id="fullName" name="fullName" value="<%= user.fullName %>" disabled required />
                </div>

                <div class="form-group">
                    <label for="email" data-translate="email_address">Email Address</label>
                    <input type="email" id="email" name="email" value="<%= user.email %>" disabled required />
                </div>

                <div class="form-group">
                    <label for="password" data-translate="password">Password</label>
                    <div class="password-field">
                        <input type="password" id="password" name="password" value="<%= user.userPasswordKey %>"
                            disabled required style="border: none; outline: none; flex-grow: 1;">
                        <i class="fas fa-eye" id="togglePassword"></i>
                    </div>
                </div>
            </div>

            <!-- Buttons -->
            <div>
                <button type="button" id="editBtn" data-translate="edit_profile">Edit Profile</button>
                <button type="submit" id="updateProfileBtn" style="display: none;"
                    data-translate="update">Update</button>
            </div>
            <% if (user && (user.role !=='affiliate' || !user.isTvStation)) { %>
                <% const showAffiliateSection=user.role !=='affiliate' ; const showTvStationSection=!user.isTvStation;
                    const showDivider=showAffiliateSection || showTvStationSection; %>

                    <% if (showDivider) { %>
                        <hr class="profile-divider">

                        <div class="request-buttons">
                            <% if (showAffiliateSection) { %>
                                <% if (!isAffiliateRequestAlreadySent) { %>
                                    <button type="button" id="makeAffiliateRequestBtn" class="request-btn"
                                        style="margin-bottom: 10px;" data-translate="make_affiliate_request">
                                        Make Affiliate Request
                                    </button>
                                    <% } else { %>
                                        <span class="request-sent-label">Affiliate Request Already Sent</span>
                                        <% } %>
                                            <% } %>

                                                <% if (showTvStationSection) { %>
                                                    <% if (!isTvStationUserRequestAlreadySent) { %>
                                                        <button type="button" id="makeTvStationRequestBtn"
                                                            class="request-btn" data-translate="make_tvstation_request">
                                                            Make TV Station User Request
                                                        </button>
                                                        <% } else { %>
                                                            <span class="request-sent-label">TV Station Request Already
                                                                Sent</span>
                                                            <% } %>
                                                                <% } %>
                        </div>
                        <% } %>
                            <% } else { %>
                                <p>Please log in to make requests.</p>
                                <% } %>



        </form>
    </div>


</body>
<script src="https://cdnjs.cloudflare.com/ajax/libs/toastify-js/1.12.0/toastify.min.js"></script>
<script src="/js/toastify-setup.js"></script>
<script src="/js/profile.js"></script>
<script>
    // Check if there's a message passed from the server
    <% if (typeof message !== 'undefined' && message) { %>
        showToast("<%= message %>", "<%= type || 'success' %>");
    <% } %>
</script>

</html>